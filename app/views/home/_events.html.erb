<div id="home-specials">
	<% @events.each do |e| %>
		<div>
			<p><%= e.name %></p>
			<p><%= link_to e.bar.name, e.bar %></p>
			<p><%= e.description %></p>
			<p><%= link_to image_tag(e.flyer.url(:small), :size => '300x300'), event_path(e) %></p>
		</div>
	<% end %>
</div>