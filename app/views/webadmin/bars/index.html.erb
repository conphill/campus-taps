<h1>Listing bars</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Address</th>
    <th>City</th>
    <th>State</th>
    <th>Zip</th>
    <th>Phone</th>
    <th></th>
    <th></th>
  </tr>

<% @bars.each do |bar| %>
  <tr>
    <td><%= link_to bar.name, [:webadmin,bar] %></td>
    <td><%= bar.description %></td>
    <td><%= bar.address %></td>
    <td><%= bar.city %></td>
    <td><%= bar.state.state_abbr %></td>
    <td><%= bar.zip %></td>
    <td><%= bar.phone %></td>
    <td><%= link_to 'Edit', edit_webadmin_bar_path(bar) %></td>
    <td><%= link_to 'Destroy', [:webadmin,bar], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bar', new_webadmin_bar_path %>
