<h1>Restaurants</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Phone</th>
	<th>Hours</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @restaurants.each do |restaurant| %>
  <tr>
    <td><%= restaurant.name %></td>
    <td><%= restaurant.address %></td>
    <td><%= restaurant.phone %></td>
	<td><%= restaurant.hours %></td>
    <td><%= link_to 'Show', [:webadmin,restaurant] %></td>
    <td><%= link_to 'Edit', edit_webadmin_restaurant_path(restaurant) %></td>
    <td><%= link_to 'Destroy', [:webadmin,restaurant], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Restaurant', new_webadmin_restaurant_path %>
